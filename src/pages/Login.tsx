import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useTranslation } from 'react-i18next'; import { Eye, EyeOff } from 'lucide-react'; import { useLoginMutation } from '../api'; import { branding } from '../config';
export function Login(){const {t}=useTranslation();const nav=useNavigate();const [login,{isLoading}]=useLoginMutation();const [error,setError]=useState('');const [showPassword,setShowPassword]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await login({email:String(f.get('email')),password:String(f.get('password'))}).unwrap();nav('/')}catch{setError('Invalid email or password')}}return <div className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#fde7b0,#fffaf0_55%)] p-4"><form onSubmit={submit} className="card w-full max-w-md p-8"><div className="mb-6 text-center"><img src={branding.logoUrl} alt="MandirLekha logo" className="mx-auto mb-3 h-28 w-28 object-contain"/><h1 className="text-2xl font-bold text-maroon">{branding.appName}</h1><p className="mt-1 font-medium">{branding.templeName}</p><p className="text-sm text-stone-500">{branding.templeAddress}</p><p className="mt-3 text-stone-500">{t('signIn')}</p></div>{error&&<p className="mb-4 rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}<label className="label">{t('email')}</label><input className="input mb-4" name="email" type="email" required autoComplete="username"/><label className="label">{t('password')}</label><div className="relative mb-6"><input className="input pr-12" name="password" type={showPassword?'text':'password'} required autoComplete="current-password"/><button type="button" onClick={()=>setShowPassword(value=>!value)} className="absolute inset-y-0 right-0 flex w-12 items-center justify-center text-stone-500 transition hover:text-maroon focus:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-saffron" aria-label={showPassword?'Hide password':'Show password'} aria-pressed={showPassword}>{showPassword?<EyeOff size={20}/>:<Eye size={20}/>}</button></div><button className="btn-primary w-full" disabled={isLoading}>{isLoading?'Signing in…':'Login / लॉग इन'}</button></form></div>}
